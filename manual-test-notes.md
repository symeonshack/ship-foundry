## RTS GAMEPLACE IMPROVEMENTS:

it's really hard to navigate the top down RTS game view.

the click and drag while top down view sometimes rotates view, sometimes navigates the map - can we make it default navigate map and then hold a key and move mouse to rotate the view? 
top down view doesn't look or feel like an RTS, it feels like a 3D 1st person game but zoomed out. can we just make a toggle RTS mode? (don't call it that though but suggest and add some simpler control method so i can easily be in RTS game play mode, truthfully i dont really care aboutt the 3D roll/pan view mode or the 1st person mode when on the site at this time, so we could just disable those for now but i'll do whatever you recommend)

need a minimap, need a "go to lander" shortcut











